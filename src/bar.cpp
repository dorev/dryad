#include "bar.h"
#include "dryadutils.h"

namespace dryad
{

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

bar_t::bar_t()
{
    _progression.reserve(MAX_CHORDS_BY_BAR);
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

void bar_t::insert_note(int offset, int duration)
{
    _voice.add_note(offset, duration);
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

void bar_t::insert_note(const note_t& single_note)
{
    _voice.add_note(single_note);
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

}