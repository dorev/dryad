#include "phraselayout.h"
#include "dryadutils.h"

namespace dryad
{

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

phrase_layout::phrase_layout(size_t bar_count, structural_pattern preffered_pattern)
    : _preffered_pattern(preffered_pattern)
{
    _bars.reserve(bar_count);
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

void phrase_layout::apply_progression(const progression& /*degrees*/)
{
    std::cout << "";
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

bar_layout& phrase_layout::operator[](size_t index)
{
    if (index > _bars.size() - 1)
    {
        CRASHLOG("Out of bound");
    }

    return _bars[index];
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

}